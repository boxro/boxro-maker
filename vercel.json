{
  "framework": "nextjs",
  "buildCommand": "rm -rf node_modules package-lock.json && npm config set registry https://registry.npmjs.org/ && npm install && npm run build",
  "installCommand": "rm -rf node_modules package-lock.json && npm config set registry https://registry.npmjs.org/ && npm install"
}
