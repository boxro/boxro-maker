rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 긴급: 모든 권한 허용 (개발/테스트용)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}











