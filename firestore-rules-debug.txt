rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 임시 디버깅용 - 모든 인증된 사용자에게 모든 권한 부여
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
